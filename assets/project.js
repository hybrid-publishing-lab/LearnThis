$(document).ready( function(){
  // autogrow textareas
  $('textarea').autogrow({onInitialize: true});

  $('textarea').focus(function(event){
    // Dimm all other text elements
  });

  $('.comment-button').click(function(event){
    // insert comment textarea

    // focus comment textarea

    // replace comment button by delete comment button
  });
});